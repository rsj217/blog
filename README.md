# blog


Install 

```shell
$ git clone git@github.com:rsj217/blog.git
$ cd blog
$ python3 -m venv venv
$ source venv/bin/activate
$ pip3 install -r requirements.txt
```

Build

```shell
$ cd docs && make html
$ open index.html
```
